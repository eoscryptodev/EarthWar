/*
let process: NodeJS.Process;
let blockchain = 'solana--devnet--rpc.datahub.figment.io'
let apiKey = 'f4c50ec23a922eb26fc7f15b835bd2ef'

// Datahub Node's RPC URL
export const getNodeRpcURL = () => {
  return process.env.blockchain === "true"
    ? `https://${process.env.blockchain}/apikey/${process.env.apiKey}`
    : process.env.blockchain;
}
*/
