import { ConfigState } from '../../../Shared/interfaces/interfaces';

/**
 * Initial Configuration State for the game. Launched by Boot State
 */
export const initialConfigState: ConfigState = {
  config: null
};
