import { CharacterState } from '../../../Shared/interfaces/interfaces';

/**
 * Initial Character State for the Game
 */
export const initialCharacterState: CharacterState = {
  player: null,
  character: null,
  active: false
};

