import { GameState } from '../../../Shared/interfaces/interfaces';

/**
 * Initial Game State for Earth War. Represents the beginning of the
 * gameplay cycle.
 */
export const initialGameState: GameState = {
  activeCharacter: null,
  lastScene: null
};
